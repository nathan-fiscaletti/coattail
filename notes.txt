
$ coattail token purge                                                 !! NEW !!

#4e88e6 -- blue
#e64e4e -- red
#6ce64e -- green
#e6d74e -- yellow

# Actions
$ coattail action queue \                                              !! NEW !!
                  --peer <peer-id> \                                   !! NEW !!
                  --action <action-id>|<action-name> \                 !! NEW !!
                  --data <data>                                        !! NEW !!
$ coattail action validate \                                           !! NEW !!
                  --action <action-id>|<action-name>                   !! NEW !!

Add an option to commands to have machine readable output              !! NEW !!
Packet permissions based on opcode                                     !! NEW !!

?? Commands for Schemas ??                                             !! NEW !!

module.exports = {
    Schemas: {}
};

Other Project Ideas:

File sync utility
Data streaming from scratch
Pick one encryption standard and implement it from scratch

Update subscription to not allow duplicates
Double check error handling
Apparently schema validation isn't working properly?
Validate receiver schema
Move actions directory to configurable directory
Make authentication keypair different than validation keypair.


Client -> Server: action, receiver
Server -> Client: Get Validation token: (action, receiver)
Client -> Server: Validation token: (action, receiver, validationToken)
Server -> Client: Get Subscription token: (action, receiver, validationToken)
Client -> Server: Subscription token: (action, receiver, validationToken, subscriptionToken)
